<div class="container">
  <h2>Teacher: <%= @teacher.name %></h2>
  <% @students.each do |student| %>
    <li class="list-group-item">
      <span>Student #<a href="/students/<%= student.id %>/edit"><%= student.id %> <%= student.name %></a></span>
    </li>
  <% end %>
</div>

